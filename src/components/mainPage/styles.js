import styled from "styled-components";

export const CodePageContainer = styled.div`
  padding: 40px;
  width: fit-content;
  height: fit-content;
  border-radius: 10px;
  background-color: #ffffff80;
  margin: 30px auto 0;
`;

export const WinText = styled.p`
  margin-top: 0;
  font-weight: 700;
  color: green;
`;

export const LoseText = styled.p`
  margin-top: 0;
  font-weight: 700;
  color: red;
`;
